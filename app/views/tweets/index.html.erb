<div class="contents row">
  <% @tweets.each do |tweet| %>
    <div class="content_post">
      <p><%= tweet.manth %>月<%= tweet.day %>日</p>
      <span class="name">
        <%= tweet.wake_up_hour %>時<%= tweet.wake_up_minute %>分
      </span>
    </div>
  <% end %>
</div>